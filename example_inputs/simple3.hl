// all optimization techniques applied (multi-pass optimization, constant propagation, constant folding, common subexpression elimination, and dead code elimination)
i = 2
while i != 8 {
    k = 1
    a = 2
    t = a + k
    r = a + k
    f = t + r
    i = i + f
    register_op FC (in: 4, out: 6)
}